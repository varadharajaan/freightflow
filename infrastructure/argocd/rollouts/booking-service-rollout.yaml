# =============================================================================
# FreightFlow Booking Service — Argo Rollout (Canary)
# =============================================================================
# Replaces the standard Deployment with a progressive canary rollout.
# Pod template mirrors infrastructure/kubernetes/base/booking-service.yaml.
# Port: 8081
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: booking-service
  namespace: freightflow
  labels:
    app: booking-service
    app.kubernetes.io/name: booking-service
    app.kubernetes.io/part-of: freightflow
spec:
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
        app.kubernetes.io/name: booking-service
        app.kubernetes.io/part-of: freightflow
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: booking-service
      containers:
        - name: booking-service
          image: ghcr.io/varadharajaan/freightflow/booking-service:latest
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: booking-db-credentials
                  key: url
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: booking-db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: booking-db-credentials
                  key: password
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: kafka-config
                  key: bootstrap-servers
            - name: EUREKA_HOST
              value: "discovery-server"
            - name: KEYCLOAK_ISSUER_URI
              valueFrom:
                configMapKeyRef:
                  name: keycloak-config
                  key: issuer-uri
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger-collector:4318/v1/traces"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
  strategy:
    canary:
      canaryService: booking-service-canary
      stableService: booking-service-stable
      trafficRouting:
        istio:
          virtualServices:
            - name: booking-service-vs
              routes:
                - primary
      steps:
        - setWeight: 10
        - pause: {duration: 2m}
        - setWeight: 30
        - pause: {duration: 2m}
        - setWeight: 60
        - pause: {duration: 2m}
        - setWeight: 100
      analysis:
        templates:
          - templateName: success-rate
          - templateName: latency-check
        startingStep: 2
        args:
          - name: service-name
            value: booking-service
          - name: namespace
            value: freightflow
---
# Stable Service — receives production traffic
apiVersion: v1
kind: Service
metadata:
  name: booking-service-stable
  namespace: freightflow
  labels:
    app: booking-service
    role: stable
spec:
  selector:
    app: booking-service
  ports:
    - port: 8081
      targetPort: 8081
      name: http
---
# Canary Service — receives canary traffic
apiVersion: v1
kind: Service
metadata:
  name: booking-service-canary
  namespace: freightflow
  labels:
    app: booking-service
    role: canary
spec:
  selector:
    app: booking-service
  ports:
    - port: 8081
      targetPort: 8081
      name: http
---
# Istio VirtualService for traffic splitting
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: booking-service-vs
  namespace: freightflow
spec:
  hosts:
    - booking-service
  http:
    - name: primary
      route:
        - destination:
            host: booking-service-stable
            port:
              number: 8081
          weight: 100
        - destination:
            host: booking-service-canary
            port:
              number: 8081
          weight: 0
