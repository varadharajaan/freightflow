# =============================================================================
# FreightFlow Notification Service — Argo Rollout (Canary)
# =============================================================================
# Pod template mirrors infrastructure/kubernetes/base/notification-service.yaml.
# Port: 8086
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: notification-service
  namespace: freightflow
  labels:
    app: notification-service
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/part-of: freightflow
spec:
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        app.kubernetes.io/name: notification-service
        app.kubernetes.io/part-of: freightflow
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8086"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: freightflow
      containers:
        - name: notification-service
          image: ghcr.io/varadharajaan/freightflow/notification-service:latest
          ports:
            - containerPort: 8086
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: notification-db-credentials
                  key: url
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: notification-db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: notification-db-credentials
                  key: password
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: kafka-config
                  key: bootstrap-servers
            - name: EUREKA_HOST
              value: "discovery-server"
            - name: SMTP_HOST
              valueFrom:
                configMapKeyRef:
                  name: smtp-config
                  key: host
            - name: SMTP_PORT
              valueFrom:
                configMapKeyRef:
                  name: smtp-config
                  key: port
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger-collector:4318/v1/traces"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
  strategy:
    canary:
      canaryService: notification-service-canary
      stableService: notification-service-stable
      trafficRouting:
        istio:
          virtualServices:
            - name: notification-service-vs
              routes:
                - primary
      steps:
        - setWeight: 10
        - pause: {duration: 2m}
        - setWeight: 30
        - pause: {duration: 2m}
        - setWeight: 60
        - pause: {duration: 2m}
        - setWeight: 100
      analysis:
        templates:
          - templateName: success-rate
          - templateName: latency-check
        startingStep: 2
        args:
          - name: service-name
            value: notification-service
          - name: namespace
            value: freightflow
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service-stable
  namespace: freightflow
  labels:
    app: notification-service
    role: stable
spec:
  selector:
    app: notification-service
  ports:
    - port: 8086
      targetPort: 8086
      name: http
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service-canary
  namespace: freightflow
  labels:
    app: notification-service
    role: canary
spec:
  selector:
    app: notification-service
  ports:
    - port: 8086
      targetPort: 8086
      name: http
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: notification-service-vs
  namespace: freightflow
spec:
  hosts:
    - notification-service
  http:
    - name: primary
      route:
        - destination:
            host: notification-service-stable
            port:
              number: 8086
          weight: 100
        - destination:
            host: notification-service-canary
            port:
              number: 8086
          weight: 0
