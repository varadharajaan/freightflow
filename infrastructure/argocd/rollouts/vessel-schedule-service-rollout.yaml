# =============================================================================
# FreightFlow Vessel Schedule Service — Argo Rollout (Canary)
# =============================================================================
# Pod template mirrors infrastructure/kubernetes/base/vessel-schedule-service.yaml.
# Port: 8084
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: vessel-schedule-service
  namespace: freightflow
  labels:
    app: vessel-schedule-service
    app.kubernetes.io/name: vessel-schedule-service
    app.kubernetes.io/part-of: freightflow
spec:
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: vessel-schedule-service
  template:
    metadata:
      labels:
        app: vessel-schedule-service
        app.kubernetes.io/name: vessel-schedule-service
        app.kubernetes.io/part-of: freightflow
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8084"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: freightflow
      containers:
        - name: vessel-schedule-service
          image: ghcr.io/varadharajaan/freightflow/vessel-schedule-service:latest
          ports:
            - containerPort: 8084
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: vessel-db-credentials
                  key: url
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: vessel-db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: vessel-db-credentials
                  key: password
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: kafka-config
                  key: bootstrap-servers
            - name: EUREKA_HOST
              value: "discovery-server"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger-collector:4318/v1/traces"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
  strategy:
    canary:
      canaryService: vessel-schedule-service-canary
      stableService: vessel-schedule-service-stable
      trafficRouting:
        istio:
          virtualServices:
            - name: vessel-schedule-service-vs
              routes:
                - primary
      steps:
        - setWeight: 10
        - pause: {duration: 2m}
        - setWeight: 30
        - pause: {duration: 2m}
        - setWeight: 60
        - pause: {duration: 2m}
        - setWeight: 100
      analysis:
        templates:
          - templateName: success-rate
          - templateName: latency-check
        startingStep: 2
        args:
          - name: service-name
            value: vessel-schedule-service
          - name: namespace
            value: freightflow
---
apiVersion: v1
kind: Service
metadata:
  name: vessel-schedule-service-stable
  namespace: freightflow
  labels:
    app: vessel-schedule-service
    role: stable
spec:
  selector:
    app: vessel-schedule-service
  ports:
    - port: 8084
      targetPort: 8084
      name: http
---
apiVersion: v1
kind: Service
metadata:
  name: vessel-schedule-service-canary
  namespace: freightflow
  labels:
    app: vessel-schedule-service
    role: canary
spec:
  selector:
    app: vessel-schedule-service
  ports:
    - port: 8084
      targetPort: 8084
      name: http
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: vessel-schedule-service-vs
  namespace: freightflow
spec:
  hosts:
    - vessel-schedule-service
  http:
    - name: primary
      route:
        - destination:
            host: vessel-schedule-service-stable
            port:
              number: 8084
          weight: 100
        - destination:
            host: vessel-schedule-service-canary
            port:
              number: 8084
          weight: 0
