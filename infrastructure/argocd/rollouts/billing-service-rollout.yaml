# =============================================================================
# FreightFlow Billing Service — Argo Rollout (Canary)
# =============================================================================
# Pod template mirrors infrastructure/kubernetes/base/billing-service.yaml.
# Port: 8083
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: billing-service
  namespace: freightflow
  labels:
    app: billing-service
    app.kubernetes.io/name: billing-service
    app.kubernetes.io/part-of: freightflow
spec:
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
        app.kubernetes.io/name: billing-service
        app.kubernetes.io/part-of: freightflow
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8083"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: billing-service
      containers:
        - name: billing-service
          image: ghcr.io/varadharajaan/freightflow/billing-service:latest
          ports:
            - containerPort: 8083
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: billing-db-credentials
                  key: url
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: billing-db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: billing-db-credentials
                  key: password
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: kafka-config
                  key: bootstrap-servers
            - name: EUREKA_HOST
              value: "discovery-server"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger-collector:4318/v1/traces"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
  strategy:
    canary:
      canaryService: billing-service-canary
      stableService: billing-service-stable
      trafficRouting:
        istio:
          virtualServices:
            - name: billing-service-vs
              routes:
                - primary
      steps:
        - setWeight: 10
        - pause: {duration: 2m}
        - setWeight: 30
        - pause: {duration: 2m}
        - setWeight: 60
        - pause: {duration: 2m}
        - setWeight: 100
      analysis:
        templates:
          - templateName: success-rate
          - templateName: latency-check
        startingStep: 2
        args:
          - name: service-name
            value: billing-service
          - name: namespace
            value: freightflow
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service-stable
  namespace: freightflow
  labels:
    app: billing-service
    role: stable
spec:
  selector:
    app: billing-service
  ports:
    - port: 8083
      targetPort: 8083
      name: http
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service-canary
  namespace: freightflow
  labels:
    app: billing-service
    role: canary
spec:
  selector:
    app: billing-service
  ports:
    - port: 8083
      targetPort: 8083
      name: http
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: billing-service-vs
  namespace: freightflow
spec:
  hosts:
    - billing-service
  http:
    - name: primary
      route:
        - destination:
            host: billing-service-stable
            port:
              number: 8083
          weight: 100
        - destination:
            host: billing-service-canary
            port:
              number: 8083
          weight: 0
